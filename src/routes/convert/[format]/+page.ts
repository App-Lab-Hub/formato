export async function entries() {
  return [
    { format: "json" },
    { format: "yaml" },
    { format: "toml" },
    { format: "xml" },
    { format: "csv" },
    { format: "ini" },
    { format: "md" },
    { format: "html" },
  ];
}
